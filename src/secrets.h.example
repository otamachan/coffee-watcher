#pragma once

// Copy this file to secrets.h (Git-ignored) and fill in real values.

#define WIFI_SSID      "your-ssid"
#define WIFI_PASSWORD  "your-password"

// Google AI Studio (Gemini API) key.
#define GEMINI_API_KEY  "AIzaSy..."

// Power Automate (Teams Workflow) HTTP-trigger URLs (test / production;
// switchable at runtime). The flow receives an Adaptive Card JSON in the
// request body and hands it to "Post card in a chat or channel".
#define TEAMS_WEBHOOK_URL_TEST  "https://default<env-id>.<region>.environment.api.powerplatform.com:443/powerautomate/automations/direct/workflows/<flow-id>/triggers/manual/paths/invoke?api-version=1&sp=...&sv=1.0&sig=..."
#define TEAMS_WEBHOOK_URL_PROD  "https://default<env-id>.<region>.environment.api.powerplatform.com:443/powerautomate/automations/direct/workflows/<flow-id>/triggers/manual/paths/invoke?api-version=1&sp=...&sv=1.0&sig=..."
